# 份额管理页面开发规划

## 一、需求分析

根据 `frontendv2/task.md` 的需求：

| 需求 | 描述 |
|------|------|
| 掩码显示 | 份额默认用掩码显示 |
| 关闭掩码按钮 | 用户需要在模态框强制等待3秒才能点击确认 |
| 复制按钮 | 添加复制份额功能，需加TODO提醒上线前移除 |

## 二、现有代码分析

**当前 share_page.dart**（[第189-192行](file:///f:/GolangWorkspace/CryptographyDesign/frontendv2/lib/pages/share_page.dart#L189-L192)）：

```dart
Text(
  '份额值: ${share.value.substring(0, 32)}...',
  style: const TextStyle(fontFamily: 'monospace'),
),
```

当前只是简单截取显示，没有掩码功能。

## 三、实现方案

### 3.1 份额掩码显示

修改份额值的显示逻辑，默认用 `*` 替换：

```dart
// 默认显示掩码
Text(
  '份额值: ${'*' * 32}...',
  style: const TextStyle(fontFamily: 'monospace'),
),
```

### 3.2 关闭掩码模态框

创建一个弹窗组件，包含：
- 3秒倒计时器
- 确认按钮（倒计时结束后才可点击）
- 取消按钮

### 3.3 复制按钮

添加复制功能，使用 `Clipboard.setData()`，并添加TODO注释。

## 四、修改计划

### 4.1 修改 share_page.dart

1. 添加 `flutter/services.dart` 导入（用于 Clipboard）
2. 创建 `_ShowShareDetailDialog` 组件
3. 修改列表项，添加操作按钮
4. 添加TODO注释

### 4.2 修改内容

**修改位置**：`frontendv2/lib/pages/share_page.dart`

**修改内容**：
- 添加掩码显示逻辑
- 添加关闭掩码按钮（弹出带倒计时的模态框）
- 添加复制按钮（带TODO注释）

### 4.3 组件设计

```dart
class _ShareDetailDialog extends StatefulWidget {
  final Share share;

  const _ShareDetailDialog({required this.share});

  @override
  State<_ShareDetailDialog> createState() => _ShareDetailDialogState();
}

class _ShareDetailDialogState extends State<_ShareDetailDialog> {
  int _countdown = 3;
  bool _canConfirm = false;

  @override
  void initState() {
    super.initState();
    _startCountdown();
  }

  void _startCountdown() {
    Timer.periodic(const Duration(seconds: 1), (timer) {
      if (_countdown > 0) {
        setState(() => _countdown--);
      } else {
        setState(() => _canConfirm = true);
        timer.cancel();
      }
    });
  }
}
```

## 五、安全性评估

| 需求 | 安全性影响 | 评估 |
|------|----------|------|
| 掩码显示 | 防止他人偷窥 | 提高安全性 |
| 3秒强制等待 | 防止误操作 | 提高安全性 |
| 复制按钮 | 便于调试，但存在泄露风险 | 需要TODO提醒上线前移除 |

## 六、实施步骤

1. 修改 `share_page.dart`，添加导入和组件
2. 验证构建
3. 测试功能

## 七、TODO事项

```dart
// TODO: [上线前移除] 调试用的复制按钮，请在正式上线前移除
```

## 八、修改文件清单

| 文件 | 修改内容 |
|------|----------|
| `lib/pages/share_page.dart` | 添加掩码显示、关闭掩码模态框、复制按钮 |