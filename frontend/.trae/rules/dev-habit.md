1. 在所有改动之后都必须使用在终端中执行`git commit -m`指令提交更改
2. 没有用户的最近明确同意, 禁止进行git变基、远程提交、合并分支、切换分支等操作
3. commit消息需遵循git conventional规范, 并在消息开头加上`[Trae]`前缀, 用于标识哪些提交由Agent进行
4. 完成修改后使用`flutter analyze`分析程序, 确认没有影响编译的错误后再向用户报告修改并结束对话