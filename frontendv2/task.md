## 桌面端份额管理实现
- **须知**: 后端不提供用户个人的份额查看API, 处于密码学安全需求, 后端不能向前端展示用户拥有的Auth Share和Recovery Share

请复用之前的本地份额管理链路, 制作一个可视化分页查看的列表出来, 份额默认用掩码显示

### 开发需求变更提议
- **须知**: 请自行评估安全性和用户体验, 如果会降低安全性, 可以否决任何提议

1. 给份额列表项添加一个*关闭掩码显示* 的按钮, 用户需要在模态框强制等待三秒钟才能点击确认按钮, 防止用户篡改
2. 给份额列表添加一个**复制**按钮, 便于开发者调试时复制份额; 要给这个改动加一个TODO, 提醒开发者在应用正式上线后移除这个功能